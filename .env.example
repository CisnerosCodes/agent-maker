# --- Security (Sky) ---
HIDDENLAYER_CLIENT_ID=
HIDDENLAYER_CLIENT_SECRET=            # event code AITX-2026, get key from the hackathon link
HIDDENLAYER_API_URL=            # confirm runtime-security endpoint from HL docs

# --- Inference ---
NVIDIA_INFERENCE_API_KEY=       # Nemotron via NVIDIA hosted endpoints (NEMOCLAW_PROVIDER=build)
# NVIDIA_API_BASE=https://integrate.api.nvidia.com/v1  # override for vLLM: http://localhost:8000/v1
# VLLM_ENDPOINT=http://localhost:8000/v1   # ladder goal: self-hosted vLLM

# --- CEO runtime (troublemaker) ---
ANTHROPIC_API_KEY=
MOM_SLACK_APP_TOKEN=            # xapp-... (Socket Mode: no public URL needed)
MOM_SLACK_BOT_TOKEN=            # xoxb-...

# --- Vault (Adrian) ---
AGENT_EMAIL_DOMAIN=agentcorp.dev
RESEND_API_KEY=
APIFY_TOKEN=                    # coupon: AITX_NVIDIA_CLAW_HACK
SHOPIFY_ADMIN_TOKEN=            # from the pre-created dev store
SHOPIFY_STORE_URL=

# --- Worker execution ---
# SIM_MODE=1                     # force ALL workers to simulation (stage insurance); labeled on dashboard
# WORKER_BACKEND=nvidia          # api | nvidia | cli — brain for worker inference (auto-detected from keys if unset)
# WORKER_MODEL=                  # override model slug for workers (defaults per backend)
# RESEARCH_SOURCE_URL=           # live data source the research agent fetches (default: dummyjson mens-shoes)

# --- Infra ---
DASHBOARD_PORT=4000
REGISTRY_DIR=./data
